---
title: Foundations
description: Demonstrações visuais das foundations do Pulso DS.
---

# Foundations

Foundations demonstram que a identidade do Pulso nasce de estrutura: superfícies,
tipo, ritmo, risco e assinatura numérica.

## Superfícies e marca

<div className="grid gap-4 md:grid-cols-4">
  <div className="overflow-hidden rounded-xl border bg-card">
    <div className="h-20 bg-background" />
    <div className="p-4">
      <strong>Background</strong>
      <p className="mt-1 font-mono text-xs text-muted-foreground">--background</p>
    </div>
  </div>
  <div className="overflow-hidden rounded-xl border bg-card">
    <div className="h-20 bg-card" />
    <div className="p-4">
      <strong>Card</strong>
      <p className="mt-1 font-mono text-xs text-muted-foreground">--card</p>
    </div>
  </div>
  <div className="overflow-hidden rounded-xl border bg-card">
    <div className="h-20 bg-muted" />
    <div className="p-4">
      <strong>Muted</strong>
      <p className="mt-1 font-mono text-xs text-muted-foreground">--muted</p>
    </div>
  </div>
  <div className="overflow-hidden rounded-xl border bg-card">
    <div className="h-20 bg-primary" />
    <div className="p-4">
      <strong>Primary</strong>
      <p className="mt-1 font-mono text-xs text-muted-foreground">--primary</p>
    </div>
  </div>
</div>

## Risco explícito

<div className="grid gap-3 md:grid-cols-5">
  {[
    ["stable", "Estável", "var(--risk-stable-bg)", "var(--risk-stable-text)"],
    ["monitor", "Monitorar", "var(--risk-monitor-bg)", "var(--risk-monitor-text)"],
    ["attention", "Atenção", "var(--risk-attention-bg)", "var(--risk-attention-text)"],
    ["high", "Alto", "var(--risk-high-bg)", "var(--risk-high-text)"],
    ["critical", "Crítico", "var(--risk-critical-bg)", "var(--risk-critical-text)"],
  ].map(([key, label, bg, color]) => (
    <div key={key} className="rounded-xl border p-4" style={{ background: bg, color }}>
      <p className="font-mono text-xs uppercase tracking-[var(--tracking-kicker)]">{key}</p>
      <strong className="mt-2 block">{label}</strong>
    </div>
  ))}
</div>

## Assinatura numérica

<div className="rounded-2xl border bg-card p-6">
  <p className="font-mono text-xs uppercase tracking-[var(--tracking-kicker)] text-muted-foreground">
    operação reputacional
  </p>
  <div className="mt-6 grid gap-6 md:grid-cols-3">
    <div>
      <strong className="mono block text-5xl font-bold" data-numeric>87</strong>
      <span className="text-sm text-muted-foreground">score de risco</span>
    </div>
    <div>
      <strong className="mono block text-5xl font-bold" data-numeric>1.482</strong>
      <span className="text-sm text-muted-foreground">menções qualificadas</span>
    </div>
    <div>
      <strong className="mono block text-5xl font-bold" data-numeric>08m</strong>
      <span className="text-sm text-muted-foreground">SLA restante</span>
    </div>
  </div>
</div>

Ver também: [Colors](/foundations/colors), [Typography](/foundations/typography) e [Density](/foundations/density).
